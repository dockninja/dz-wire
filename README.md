# DZ Contracts

This repository contains contract used in the DZ project.

## Makefile Targets

- `make gen`: Generates code from protocol buffer definitions using Buf.
- `make lint`: Lints the protocol buffer definitions using Buf.
- `make clean`: Cleans up generated code.
